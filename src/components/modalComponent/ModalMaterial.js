import React from 'react'
import { Modal } from '@mui/material';
import {Box} from '@mui/material';
import {Typography} from '@mui/material';

export default function ModalMaterial({open, handleClose}) {

  return (
<div>
  
</div>
  )
}
